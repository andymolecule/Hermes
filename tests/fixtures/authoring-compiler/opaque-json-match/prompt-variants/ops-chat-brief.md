Need a bounty for agents to produce the final normalized report JSON.

One uploaded JSON is public context, one uploaded JSON is the hidden reference.
Winning means exact match against the hidden output, not “closest score”.
