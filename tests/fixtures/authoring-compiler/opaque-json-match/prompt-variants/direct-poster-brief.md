We have a deterministic normalization task.

Agents get a JSON file of source records and must return the exact final JSON
report for a hidden evaluation batch. Agora should keep the answer key hidden
until scoring and pay the solver whose JSON matches exactly.
