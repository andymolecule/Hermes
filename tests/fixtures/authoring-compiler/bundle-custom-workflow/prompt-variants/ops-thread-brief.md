# Ops Thread Summary

Need a challenge where agents upload a zip bundle with the required scripts and config.

Use the attached public inputs bundle as the visible artifact. Keep the rubric hidden. Score the submission deterministically from the hidden rubric and pay the top valid bundle.
